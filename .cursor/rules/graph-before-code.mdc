---
description: Hard block on code edits until VaultGate runs; GraphGate only when not owner-waived
alwaysApply: true
---

# Vault before graph before code (blocking)

Vault protocol: [AGENTS.md](../../AGENTS.md) · [Memory Stack](file:///C:/Users/MSI/Desktop/Obsidian%20Vault%20From%20VPS/hft3/Memory%20Stack.md)

## Non-negotiable order

1. **VaultGate** — `scripts/vault_gate.ps1 -Query "<task-specific question>"`. Creates `runtime/vault-gate/.last-vault-gate.json`. Read `wiki/hot.md`, `Home.md`, `Memory Stack.md`, relevant `decisions/`.
2. **VaultPre** — `scripts/vault_pre_edit.ps1` (fails exit 2 if vault stamp missing or stale >4h).
3. **Graph waiver check** — If vault `wiki/hot.md` says `waived-by-owner-2026-06-16`, **skip** GraphGate/GraphPre/GraphPost; use VaultGate + targeted source reads. Do not treat Graph as substitute for Vault.
4. **GraphGate** (when active only) — `scripts/graphify_gate.ps1 -Query "..."`.
5. **GraphPre** (when active only) — `scripts/graphify_pre_edit.ps1`.
6. **Plan / locate / code / review / verify**.

## Forbidden

- Code edits without VaultPre passing
- Skipping VaultGate because GraphGate ran
- Running GraphGate when owner waiver is active (unless owner re-enabled graph in vault hot.md)

CHI404 paths: [docs/vault/CHI404_CANONICAL_ENTRYPOINTS.md](../../docs/vault/CHI404_CANONICAL_ENTRYPOINTS.md)
