"""Feature extraction and model definitions.

Sub-packages:
    features           — MBOFeatureExtractor, OrderBook, MBOEvent, feature vectors
    structural_models  — PDF-based structural market models (book pressure, VPIN, etc.)
    hypotheses         — Research hypothesis model registry and slugs
    imbalance          — Order-flow imbalance, auction imbalance, MBQ snapshots
    regimes            — Regime filters, event context engine
"""
