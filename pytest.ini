[pytest]
markers =
    integration: requires built C++ binaries (hft_research_sim, hft_feature_golden)
    slow: long-running tests (live Ollama, etc.)
    production: crypto production smokes (requires crypto_ready or CRYPTO_PRODUCTION_SMOKE=1)
